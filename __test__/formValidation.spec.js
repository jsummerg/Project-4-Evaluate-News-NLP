import { checkValid } from "../src/client/js/formValidation"

describe("Testing the validation functionality", () => {
    test("Testing the checkValid() function", () => {
           expect(checkValid).toBeDefined();
})});